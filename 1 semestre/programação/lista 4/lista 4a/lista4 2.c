#include <stdio.h>
int main() {

int v[50], x, par=0;

    printf("digite 50 numeros inteiros:\n");

    x=0;
    while(x<=49){
            printf("vetor [%i]:",x);
            scanf("%i",&v[x]);
            x++;
    }

    for(x=0; x<=49; x++){
            printf("%i\t",v[x]);
    }

    for(x=0; x=49; x++){
        if(v[x]<=0);

    }
return 0;
}
