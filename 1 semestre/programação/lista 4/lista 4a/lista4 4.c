#include <stdio.h>
#include <stdlib.h>
int main() {

    int a[80], i, x;

    for(i=0; i<80; i++){
        a[i]=rand()%100;
        printf("%i\t",a[i]);

    }
    for(a[i]=1; a[i]<=80; i++){
            scanf("%i", &i);
            if(a[i]>x);
              x=i;
    }

        printf("maior numero = %i\n", a[i]);
        printf("menor nurmero = %i\n", a[i]);

return 0;
}
